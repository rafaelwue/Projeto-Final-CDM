<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/MainConstraint"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context=".Aplicativo.view.MainActivity">

    <Button
        android:id="@+id/bt_historico"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:onClick="goToHistorico"
        android:text="Historico"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/btnCadastrarUsuario"
        app:layout_constraintVertical_bias="0.023"
        tools:ignore="TextContrastCheck"
        tools:layout_editor_absoluteX="0dp" />

    <Button
        android:id="@+id/btnListarUsuarios"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnCadastrarUsuario"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="296dp"
        android:onClick="goToUsuarios"
        android:text="@string/btnListarUsuarios"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="TextContrastCheck"
        tools:layout_editor_absoluteX="0dp" />

    <Button
        android:id="@+id/btnCadastrarUsuario"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginTop="12dp"
        android:onClick="goToCadastroUsuario"
        android:text="@string/btnCadastrarUsuarios"
        app:layout_constraintTop_toBottomOf="@+id/btnListarUsuarios"
        tools:ignore="TextContrastCheck"
        tools:layout_editor_absoluteX="-16dp" />


</androidx.constraintlayout.widget.ConstraintLayout>
